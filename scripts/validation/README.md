# Validation Scripts

Metric validation scripts live here. Keep improving existing scripts instead of creating one-off files for each task.

Run scripts from the project root unless a script documents otherwise. Real workbooks and validation outputs must remain local-only.
